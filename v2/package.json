{
  "name": "hedgepig-v2",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "The hedgepig adventure, rebuilt as a cel-shaded meadow on a small planet.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "play": "vite build && vite preview --port 5181 --open",
    "test": "node smoke.js all"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
